Ways to add dynamic content (can be different for every request) to view:
1. Inline code
2. Tag Helpers
3. Sections
4. Partial views (this is usually just for html, without additional app logic)
5. View components (this is for separate html + app logic)